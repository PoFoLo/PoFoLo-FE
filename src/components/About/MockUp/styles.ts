import styled from 'styled-components';

export const MockUpLayout = styled.div`
  margin: 43rem 0 8.2rem;
  position: relative;
  width: 153.6rem;
  height: 86.43rem;

  .macbook {
    width: 100%;
    height: 100%;
    z-index: 0;
  }

  .contents {
    position: absolute;
    top: 5.5rem;
    left: 19.6rem;
    z-index: 1;
    width: 114.9rem;
    height: 71.8rem;
  }
`;
